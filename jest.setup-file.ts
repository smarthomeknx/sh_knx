//import * as winston from "winston";

// don't log while running tests
//winston.remove(winston.transports.Console);
//winston.remove(winston.transports.File);
//import * as logging from "./src/utils/logging";

//logging.CONVERTER_LOG.remove(winston.transports.Console);
//logging.CONVERTER_LOG.remove(winston.transports.File);

//logging.LOG.remove(winston.transports.Console);
//logging.LOG.remove(winston.transports.File);
